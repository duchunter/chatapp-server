const {
  createGroup,
  getGroupInfo,
  addMember,
  updateGroupName,
  removeMember
} = require('../models');

module.exports = {
  createGroup,
  getGroupInfo,
  addMember,
  updateGroupName,
  removeMember
};
